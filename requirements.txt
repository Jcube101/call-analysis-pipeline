# Audio processing
pydub>=0.25.1
noisereduce>=3.0.0
librosa>=0.10.0
scipy>=1.10.0
numpy>=1.24.0

# Speaker diarization
pyannote.audio>=3.1.0

# Transcription (open-source Whisper, runs locally)
openai-whisper>=20231117

# PyTorch — CPU build; GPU users can swap for a CUDA wheel
# See https://pytorch.org/get-started/locally/
torch>=2.0.0
torchaudio>=2.0.0

# Config / utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
